# Руководство по запуску проекта

## Быстрый старт

### 1. Подготовка окружения

```bash
# Перейти в директорию проекта
cd /Users/den1shh/Documents/iad

# Активировать виртуальное окружение (если не активировано)
source myenv/bin/activate
```

### 2. Запуск основного анализа

**Вариант A: Базовый анализ (быстро)**
```bash
python src/script.py
```
Выведет 3 графика:
- Тренд (train vs forecast)
- Сезонность (train vs forecast)
- Комбинированный сигнал (тренд + сезонность)

**Вариант B: Расширенный анализ (подробнее)**
```bash
python src/scriptfromds.py
```
Включает дополнительную проекцию на train-базис и добавление гауссовского шума.

### 3. Создание анимации сингулярных векторов

```bash
python src/animation.py
```
Результат сохранится в `figures/ssa_singular_vectors.gif`

### 4. Интерактивное исследование в Jupyter

```bash
jupyter notebook notebooks/calcs.ipynb
```
Ноутбук позволит:
- Экспериментировать с параметрами SSA
- Выполнять вычисления пошагово
- Создавать собственные графики
- Изменять параметры на лету

---

## Описание основных параметров

В скриптах можно изменять следующие параметры:

### `script.py` и `scriptfromds.py`

```python
# Количество компонент для анализа
ssn_idx = 35  # Индекс, до которого включаются сезонные компоненты

# Количество шагов прогноза (дневных периодов)
steps = len(y_test)  # По умолчанию весь тестовый период

# Масштаб добавленного шума (scriptfromds.py)
scale = 0.5  # 0 = без шума, 1 = полный шум
```

### `animation.py`

```python
num_vectors = 100  # Количество сингулярных векторов для анимации
fps = 2           # Скорость анимации (кадры в секунду)
```

---

## Структура данных

### Входные данные (`data/eth_usdt_3min.csv`)

CSV файл с столбцами:
- `timestamp` — время открытия свечи
- `open`, `high`, `low`, `close` — OHLC цены
- `volume` — объём торговли
- Технические индикаторы (MA, RSI, MACD, Bollinger Bands и т.д.)

### Выходные данные

После запуска скриптов:

1. **Консольный вывод**:
   - Размеры матрицы Ханкеля (N, L, K)
   - Графики в окне matplotlib

2. **Сохранённые файлы** (в `figures/`):
   - `ssa_singular_vectors.gif` — анимация EOF
   - (Графики из `scriptfromds.py` могут быть сохранены добавлением `fig.savefig()`)

---

## Возможные ошибки и их решение

### Ошибка: `ModuleNotFoundError: No module named 'pandas'`
**Решение**: Установить зависимости:
```bash
pip install -r requirements.txt
```

### Ошибка: `FileNotFoundError: eth_usdt_3min.csv`
**Решение**: Убедитесь, что вы запускаете скрипты из директории `src/`, или используйте абсолютный путь.

### Ошибка: Графики не появляются
**Решение**: Убедитесь, что работает matplotlib backend:
```python
import matplotlib
matplotlib.use('TkAgg')  # или другой подходящий backend
```

---

## Дополнительные возможности

### Сохранение результатов

Чтобы сохранить графики, добавьте в конец скрипта:
```python
plt.savefig('../figures/my_plot.png', dpi=300, bbox_inches='tight')
```

### Экспорт прогноза в CSV

```python
forecast_df = pd.DataFrame({
    'time_index': np.arange(N, N + len(y_forecast)),
    'forecast': y_forecast,
    'trend': y_trend_forecast,
    'seasonality': y_season_forecast
})
forecast_df.to_csv('../figures/forecast.csv', index=False)
```

### Использование в других проектах

Можно импортировать функции SSA:
```python
import sys
sys.path.insert(0, '/path/to/iad/src')

from scriptfromds import diagonal_averaging_to_N, compute_lrr_from_Ugroup
```

---

## Нужна помощь?

- 📖 Полное описание методологии см. в `README.md`
- 💻 Примеры использования в `notebooks/calcs.ipynb`
- 🔬 Исходный код хорошо комментирован в `src/`

---

**Версия**: 1.0 (январь 2026 г.)
