# Исследование анализа сингулярного спектра (SSA) на данных Ethereum

## 📋 Описание проекта

Данный проект посвящён изучению и применению **анализа сингулярного спектра (SSA)** для анализа и прогнозирования временных рядов на примере данных цены Ethereum (ETH/USDT) в интервале 3 минуты.

### Основные цели исследования:
- 🔍 **Декомпозиция временного ряда** на компоненты тренда, сезонности и шума
- 📊 **Анализ сингулярного спектра** для выявления значимых сигналов
- 🎯 **Прогнозирование** будущих значений ряда методом LRR (Linear Recurrence Relations)
- 📈 **Визуализация** компонент и их динамики
- 🎬 **Анимация** левых сингулярных векторов (EOF)

## 📁 Структура проекта

```
iad/
├── src/                          # Исходный код
│   ├── animation.py             # Создание анимации сингулярных векторов (GIF)
│   ├── script.py                # Основной анализ SSA с разложением и прогнозом
│   └── scriptfromds.py          # Расширенная версия с детальной обработкой компонент
│
├── notebooks/                    # Jupyter-ноутбуки для интерактивного анализа
│   └── calcs.ipynb              # Пошаговое исследование SSA и спектра
│
├── data/                         # Исходные данные
│   └── eth_usdt_3min.csv        # Котировки ETH/USDT (3-минутные свечи)
│
├── figures/                      # Результаты визуализации
│   ├── spectrum.png             # Сингулярный спектр и кумулятивная дисперсия
│   ├── pricesgraph.png          # Исходный временной ряд
│   ├── dailytrend.png           # Дневной тренд цены
│   └── ssa_singular_vectors.gif # Анимация попарных сингулярных векторов
│
├── presentation/                 # Материалы презентации
│   ├── presentation.pdf         # PDF-версия презентации
│   ├── presentation.tex         # Исходный LaTeX файл
│   └── [другие файлы LaTeX]
│
├── docs/                         # Документация и описания (расширение проекта)
│
└── README.md                     # Этот файл
```

## 🔬 Теория

### Основные этапы SSA:

1. **Embedding (Вложение)**: Исходный ряд $y_1, y_2, \ldots, y_N$ преобразуется в матрицу Ханкеля $H$ размером $L \times K$:

$$H = \begin{pmatrix}y_1 & y_2 & \cdots & y_K \\y_2 & y_3 & \cdots & y_{K+1} \\\vdots & \vdots & \ddots & \vdots \\y_L & y_{L+1} & \cdots & y_N\end{pmatrix}$$

2. **SVD (Сингулярное разложение)**:

$$H = U \Sigma V^T$$

где $U$ — левые сингулярные векторы (EOF), $\Sigma$ — сингулярные значения, $V$ — правые векторы.

3. **Grouping (Группировка)**: Элементарные матрицы объединяются в группы (тренд, сезонность, шум):

$$X_k = \sqrt{\lambda_k} u_k v_k^T$$

4. **Diagonal Averaging (Диагональное усреднение)**: Восстановление ряда из сгруппированных матриц.

5. **Forecasting (Прогноз)**: Использование линейных рекуррентных соотношений (LRR) для экстраполяции.

## 📊 Основные файлы

### `src/script.py`
**Основной скрипт анализа** — выполняет полный цикл SSA анализа:
- Загрузка и предварительная обработка данных ETH/USDT
- Построение матрицы Ханкеля
- SVD разложение
- Разделение на компоненты (тренд, сезонность, шум)
- Прогноз на 44 дня вперёд с использованием LRR
- Вывод 3 графиков: тренд, сезонность, комбинированный сигнал

**Использование:**
```bash
python src/script.py
```

### `src/scriptfromds.py`
**Расширенная версия** с детальной обработкой:
- Аналогичен `script.py`, но с более подробными комментариями
- Проекция тестовых данных на train-базис для стабильности
- Добавление гауссовского шума к прогнозам
- Проверка совместимости компонент

**Использование:**
```bash
python src/scriptfromds.py
```

### `src/animation.py`
**Создание анимации** сингулярных векторов:
- Визуализирует первые 100 левых сингулярных векторов
- Показывает попарные комбинации $(U_1, U_2), (U_3, U_4)$, и т.д.
- Сохраняет результат в `figures/ssa_singular_vectors.gif`

**Использование:**
```bash
python src/animation.py
```

## 📓 Jupyter-ноутбук

`notebooks/calcs.ipynb` содержит пошаговое исследование:
- Загрузка и изучение данных
- Вычисление SSA спектра
- Анализ кумулятивной дисперсии
- Визуализация первых EOF-векторов
- Интерактивные графики

## 📈 Результаты и выводы

### Основные находки:

1. **Спектр энергии**: Первая компонента отвечает за основной тренд, компоненты 1–74 захватывают ~95% дисперсии.

2. **Сезонность**: Чётко выраженные парные компоненты с периодом, соответствующим дневным и недельным циклам.

3. **Прогноз**: LRR-метод позволяет предсказывать тренд на 40–50 точек (40–50 дней в дневном разрезе).

4. **Качество восстановления**: Реконструкция исходного сигнала из групп компонент достаточно точна для анализа основных движений цены.

## 🛠️ Установка и запуск

### Требования:
- Python 3.8+
- pandas, numpy, matplotlib
- Jupyter (для ноутбуков)

### Установка виртуального окружения:
```bash
python -m venv myenv
source myenv/bin/activate  # Linux/macOS
# или
myenv\Scripts\activate     # Windows
```

### Установка зависимостей:
```bash
pip install pandas numpy matplotlib jupyter scikit-learn
```

### Запуск основного анализа:
```bash
cd /Users/den1shh/Documents/iad
source myenv/bin/activate
python src/script.py
```

### Запуск Jupyter:
```bash
jupyter notebook notebooks/calcs.ipynb
```

## 📚 Использованные источники и методы

- **SSA методология**: Broomhead, D. S., & King, G. P. (1986) — "Extracting qualitative dynamics from experimental data"
- **Диагональное усреднение**: Golyandina, N., Nekrutkin, V., & Zhigljavsky, A. (2001)
- **LRR прогноз**: Vautard, R., & Ghil, M. (1989) — "Singular spectrum analysis in nonlinear dynamics"

## 📝 Автор и лицензия

Исследовательский проект SSA на данных криптовалют.

---

## 🔗 Как использовать результаты

1. **Для анализа**: Откройте `notebooks/calcs.ipynb` в Jupyter и экспериментируйте с параметрами.
2. **Для воспроизведения**: Запустите `src/script.py` или `src/scriptfromds.py`.
3. **Для визуализации**: Результаты (PNG, GIF) сохраняются в `figures/`.

---

**Последнее обновление**: январь 2026 г.
